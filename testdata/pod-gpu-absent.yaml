apiVersion: v1
kind: Pod
metadata:
  name: gpu-pod
  namespace: default
spec:
  containers:
  - name: trainer
    image: busybox
    resources:
      requests:
        cpu: "2"
        nvidia.com/gpu: "1"
