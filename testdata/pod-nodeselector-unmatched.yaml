apiVersion: v1
kind: Pod
metadata:
  name: needs-special-node
  namespace: default
spec:
  nodeSelector:
    hardware: quantum
  containers:
  - name: app
    image: busybox
    resources:
      requests:
        cpu: "1"
